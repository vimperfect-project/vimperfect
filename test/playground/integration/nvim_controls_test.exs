defmodule Vimperfect.Playground.Editor.NvimControlsTest do
  @moduledoc """
  Tests how the editor controls interact with the filesystem and nvim instance
  """
  use ExUnit.Case, async: false

  # TODO: Make sure that editor controls properly control nvim instance
end
